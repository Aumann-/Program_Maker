This is a simple program and shell that to make a new program as a test for machine created programs.

When the help.bat file is run, test_out.cpp is compiled and test_out.exe is ran. This program will generate a new program.
test_out.exe will generate a new .cpp file called o.cpp. This file is then compiled and ran which will display text to the screen in a loop.